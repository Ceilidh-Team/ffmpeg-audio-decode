{
  "name": "ffmpeg-audio-decode",
  "version": "0.0.0",
  "description": "Decode arbitrary audio to PCM using FFmpeg",
  "main": "build/Release/binding.node",
  "types": "types/index.d.ts",
  "author": "JustANull",
  "license": "Dual licensed under Apache 2.0 or the MIT license",
  "private": true,
  "scripts": {
    "lint:ts": "tslint --project types --config types/tslint.json",
    "lint:js": "standard \"test/**.js\"",
    "lint": "npm run lint:ts && npm run lint:js",
    "pretest": "npm run lint",
    "test": "mocha",
    "pretest-fulltrace": "npm run pretest",
    "test-fulltrace": "mocha --full-trace"
  },
  "devDependencies": {
    "ajv": "^5.0.0",
    "mocha": "^5.2.0",
    "node-gyp": "^3.7.0",
    "should": "^13.2.1",
    "standard": "^11.0.1",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^7.1.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "@types/node": "^10.5.2"
  }
}
