{
  "name": "ffmpeg-audio-decode",
  "version": "0.0.0",
  "description": "Decode arbitrary audio to PCM using ffmpeg",
  "main": "src/js/index",
  "author": "JustANull",
  "license": "Dual licensed under Apache 2.0 or the MIT license",
  "private": true,
  "scripts": {
    "lint": "standard \"src/js/**.js\" && tslint --project types",
    "lint-fix": "standard --fix \"src/js/**.js\" && tslint --fix --project types",
    "pretest": "node-gyp rebuild",
    "test": "npm run lint && mocha",
    "pretest-fulltrace": "npm run pretest",
    "test-fulltrace": "npm run lint && mocha --full-trace"
  },
  "devDependencies": {
    "@types/node": "^10.5.2",
    "ajv": "^5.0.0",
    "mocha": "^5.2.0",
    "node-gyp": "^3.7.0",
    "should": "^13.2.1",
    "standard": "^11.0.1",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^7.1.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "bindings": "^1.3.0"
  }
}
