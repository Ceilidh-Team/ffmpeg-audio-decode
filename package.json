{
  "name": "ffmpeg-audio-decode",
  "version": "0.0.0",
  "description": "Decode arbitrary audio to PCM using FFmpeg",
  "main": "build/Release/binding.node",
  "types": "types",
  "author": "JustANull",
  "license": "Dual licensed under Apache 2.0 or the MIT license",
  "private": true,
  "scripts": {
    "lint": "standard \"test/**.js\" && dtslint types",
    "pretest": "node-gyp rebuild && npm run lint",
    "test": "mocha",
    "pretest-fulltrace": "npm run pretest",
    "test-fulltrace": "mocha --full-trace"
  },
  "devDependencies": {
    "@types/node": "^10.5.2",
    "ajv": "^5.0.0",
    "dtslint": "^0.3.0",
    "mocha": "^5.2.0",
    "node-gyp": "^3.7.0",
    "should": "^13.2.1",
    "standard": "^11.0.1",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "bindings": "^1.3.0"
  }
}
