{
  "name": "ffmpeg-audio-decode",
  "version": "0.0.0",
  "description": "Decode arbitrary audio to PCM using ffmpeg",
  "main": "src/js/index",
  "author": "JustANull",
  "license": "Dual licensed under Apache 2.0 or the MIT license",
  "private": true,
  "scripts": {
    "lint": "standard",
    "lint-fix": "standard --fix",
    "pretest": "node-gyp rebuild",
    "test": "npm run lint && why-is-node-running node_modules/mocha/bin/_mocha",
    "pretest-fulltrace": "npm run pretest",
    "test-fulltrace": "npm run lint && mocha --full-trace"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "node-gyp": "^3.7.0",
    "should": "^13.2.1",
    "standard": "^11.0.1",
    "why-is-node-running": "^2.0.2"
  },
  "dependencies": {
    "bindings": "^1.3.0"
  }
}
